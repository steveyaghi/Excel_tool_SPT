<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SPT Form Assistant</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            gap: 20px;
            padding: 20px;
        }

        .container {
            display: flex;
            max-width: 1400px;
            margin: 0 auto;
            gap: 20px;
            width: 100%;
        }

        .form-panel {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            flex: 1;
            max-height: 90vh;
            overflow-y: auto;
        }

        .chat-panel {
            background: white;
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            width: 400px;
            display: flex;
            flex-direction: column;
            max-height: 90vh;
        }

        h1 {
            color: #2c3e50;
            margin-bottom: 30px;
            text-align: center;
        }

        h2 {
            color: #34495e;
            margin-bottom: 20px;
            padding: 10px;
            background: #ecf0f1;
            border-radius: 8px;
        }

        .form-section {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            border-left: 4px solid #3498db;
        }

        .field-group {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 15px;
            align-items: center;
            margin-bottom: 15px;
        }

        label {
            font-weight: 600;
            color: #2c3e50;
        }

        input, select {
            padding: 10px;
            border: 2px solid #bdc3c7;
            border-radius: 8px;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        input:focus, select:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
        }

        .unit {
            font-size: 12px;
            color: #7f8c8d;
            margin-left: 5px;
        }

        /* Chat Styles */
        .chat-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .chat-messages {
            flex: 1;
            overflow-y: auto;
            padding: 10px;
            background: #f8f9fa;
            border-radius: 10px;
            margin-bottom: 15px;
            max-height: 400px;
        }

        .message {
            margin-bottom: 15px;
            padding: 12px 15px;
            border-radius: 18px;
            max-width: 85%;
            line-height: 1.4;
        }

        .user-message {
            background: #3498db;
            color: white;
            margin-left: auto;
            border-bottom-right-radius: 5px;
        }

        .ai-message {
            background: #ecf0f1;
            color: #2c3e50;
            margin-right: auto;
            border-bottom-left-radius: 5px;
        }

        .chat-input-container {
            display: flex;
            gap: 10px;
        }

        .chat-input {
            flex: 1;
            padding: 12px;
            border: 2px solid #bdc3c7;
            border-radius: 25px;
            outline: none;
        }

        .chat-input:focus {
            border-color: #3498db;
        }

        .send-btn {
            background: #3498db;
            color: white;
            border: none;
            border-radius: 50%;
            width: 45px;
            height: 45px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .send-btn:hover {
            background: #2980b9;
            transform: scale(1.05);
        }

        .loading {
            display: none;
            text-align: center;
            color: #7f8c8d;
            font-style: italic;
        }

        .export-btn {
            background: #27ae60;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 20px;
            transition: all 0.3s ease;
        }

        .export-btn:hover {
            background: #229954;
            transform: translateY(-2px);
        }

        .status-indicator {
            padding: 8px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            margin-bottom: 15px;
            text-align: center;
        }

        .status-ready {
            background: #d5f4e6;
            color: #27ae60;
        }

        .status-incomplete {
            background: #ffeaa7;
            color: #fdcb6e;
        }

        @media (max-width: 1024px) {
            .container {
                flex-direction: column;
            }
            
            .chat-panel {
                width: 100%;
                order: -1;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Excel Form Panel -->
        <div class="form-panel">
            <h1>SPT Form Assistant</h1>
            
            <div class="form-section">
                <h2>📋 Setup Sheet</h2>
                
                <div class="field-group">
                    <label for="project">Project:</label>
                    <input type="text" id="project" placeholder="Enter project name">
                </div>
                
                <div class="field-group">
                    <label for="project_number">Project Number:</label>
                    <input type="text" id="project_number" placeholder="Enter project number">
                </div>
                
                <div class="field-group">
                    <label for="designer">Designer:</label>
                    <input type="text" id="designer" placeholder="Enter designer name">
                </div>
                
                <div class="field-group">
                    <label for="point_load">Point Load:</label>
                    <div>
                        <input type="number" id="point_load" placeholder="100">
                        <span class="unit">kN</span>
                    </div>
                </div>
                
                <div class="field-group">
                    <label for="min_bed_level">Min Bed Level:</label>
                    <div>
                        <input type="number" id="min_bed_level" step="0.01" placeholder="-6.55">
                        <span class="unit">mNAP</span>
                    </div>
                </div>
                
                <div class="field-group">
                    <label for="max_bed_level">Max Bed Level:</label>
                    <div>
                        <input type="number" id="max_bed_level" step="0.01" placeholder="-6.55">
                        <span class="unit">mNAP</span>
                    </div>
                </div>
                
                <div class="field-group">
                    <label for="material">Material:</label>
                    <select id="material">
                        <option value="">Select material...</option>
                        <option value="S235">S235</option>
                        <option value="S275">S275</option>
                        <option value="S355">S355</option>
                        <option value="S420">S420</option>
                        <option value="S450">S450</option>
                        <option value="API 5LB">API 5LB</option>
                        <option value="API X42">API X42</option>
                        <option value="API X46">API X46</option>
                        <option value="API X52">API X52</option>
                        <option value="API X56">API X56</option>
                        <option value="API X60">API X60</option>
                        <option value="API X65">API X65</option>
                        <option value="API X70">API X70</option>
                        <option value="C12/15">C12/15</option>
                        <option value="C16/20">C16/20</option>
                        <option value="C20/25">C20/25</option>
                        <option value="C30/37">C30/37</option>
                        <option value="C35/45">C35/45</option>
                        <option value="C40/50">C40/50</option>
                        <option value="C45/55">C45/55</option>
                    </select>
                </div>
                
                <div class="field-group">
                    <label for="diameter">Diameter:</label>
                    <div>
                        <input type="number" id="diameter" step="0.001" placeholder="0.914">
                        <span class="unit">m</span>
                    </div>
                </div>
                
                <div class="field-group">
                    <label for="wall_thickness">Wall Thickness:</label>
                    <div>
                        <input type="number" id="wall_thickness" step="0.001" placeholder="0.025">
                        <span class="unit">m</span>
                    </div>
                </div>
            </div>
            
            <div class="form-section">
                <h2>🏗️ SP1 Sheet</h2>
                
                <div class="field-group">
                    <label for="spring_type">Spring Type:</label>
                    <select id="spring_type">
                        <option value="">Select spring type...</option>
                        <option value="Brinch Hansen (Dsheet)">Brinch Hansen (Dsheet)</option>
                        <option value="Matlock Soft Clay">Matlock Soft Clay</option>
                        <option value="Reese et al Stiff Clay - FW">Reese et al Stiff Clay - FW</option>
                        <option value="Reese & Welch Stiff Clay">Reese & Welch Stiff Clay</option>
                        <option value="API Sand">API Sand</option>
                        <option value="Strong Rock">Strong Rock</option>
                        <option value="Weak Rock">Weak Rock</option>
                        <option value="phi_c_Soil">phi_c_Soil</option>
                    </select>
                </div>
                
                <div class="field-group">
                    <label for="friction_angle">Friction Angle:</label>
                    <div>
                        <input type="number" id="friction_angle" placeholder="30">
                        <span class="unit">degrees</span>
                    </div>
                </div>
            </div>
            
            <button class="export-btn" onclick="exportData()">📄 Export to Excel</button>
        </div>
        
        <!-- Chat Panel -->
        <div class="chat-panel">
            <div class="chat-header">
                <h2>💬 AI Assistant</h2>
                <div id="status-indicator" class="status-indicator status-ready">
                    Ready to help!
                </div>
            </div>
            
            <div class="chat-messages" id="chat-messages">
                <div class="message ai-message">
                    👋 Hi! I'm your SPT form assistant. You can tell me things like:
                    <br><br>
                    • "Set project to Harbor Extension"
                    • "Use S355 material with 1.2m diameter" 
                    • "Update bed levels from -8.5 to -7.2 mNAP"
                    <br><br>
                    Just describe what you need in natural language!
                </div>
            </div>
            
            <div class="loading" id="loading">AI is thinking...</div>
            
            <div class="chat-input-container">
                <input 
                    type="text" 
                    class="chat-input" 
                    id="chat-input" 
                    placeholder="Tell me what to update..."
                    onkeypress="handleKeyPress(event)"
                >
                <button class="send-btn" onclick="sendMessage()">
                    ➤
                </button>
            </div>
        </div>
    </div>

    <script>
        // Configuration - Update these URLs for your setup
        const WEBHOOK_URL = 'https://your-n8n-webhook-url.com/webhook/spt-chat';
        
        // Form field mappings
        const fieldMappings = {
            'project': 'project',
            'project_number': 'project_number', 
            'designer': 'designer',
            'point_load': 'point_load',
            'min_bed_level': 'min_bed_level',
            'max_bed_level': 'max_bed_level',
            'material': 'material',
            'diameter': 'diameter',
            'wall_thickness': 'wall_thickness',
            'spring_type': 'spring_type',
            'friction_angle': 'friction_angle'
        };

        async function sendMessage() {
            const input = document.getElementById('chat-input');
            const message = input.value.trim();
            
            if (!message) return;
            
            // Add user message to chat
            addMessage(message, 'user');
            input.value = '';
            
            // Show loading
            showLoading(true);
            
            try {
                // Get current form data as context
                const currentData = getCurrentFormData();
                
                // Send to webhook
                const response = await fetch(WEBHOOK_URL, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        message: message,
                        current_data: currentData,
                        timestamp: new Date().toISOString()
                    })
                });

                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }

                const result = await response.json();
                
                // Process AI response
                handleAIResponse(result);
                
            } catch (error) {
                console.error('Error:', error);
                addMessage('❌ Sorry, I had trouble processing that. Please try again or check the connection.', 'ai');
                updateStatus('error', 'Connection error');
            } finally {
                showLoading(false);
            }
        }

        function handleAIResponse(result) {
            // Add AI message to chat
            const message = result.message || result.response || 'I updated the form based on your request.';
            addMessage(message, 'ai');
            
            // Update form fields if data provided
            if (result.extracted_data) {
                updateFormFields(result.extracted_data);
                updateStatus('success', 'Form updated!');
            } else if (result.status === 'incomplete') {
                updateStatus('incomplete', 'Need more info');
            } else if (result.status === 'complete') {
                updateStatus('success', 'Ready to export!');
            }
        }

        function updateFormFields(data) {
            Object.keys(data).forEach(field => {
                const element = document.getElementById(field);
                if (element && data[field] !== null && data[field] !== '') {
                    element.value = data[field];
                    
                    // Add visual feedback
                    element.style.backgroundColor = '#d5f4e6';
                    setTimeout(() => {
                        element.style.backgroundColor = '';
                    }, 2000);
                }
            });
        }

        function getCurrentFormData() {
            const data = {};
            Object.keys(fieldMappings).forEach(field => {
                const element = document.getElementById(field);
                if (element && element.value) {
                    data[field] = element.value;
                }
            });
            return data;
        }

        function addMessage(text, sender) {
            const messagesContainer = document.getElementById('chat-messages');
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${sender}-message`;
            messageDiv.innerHTML = text;
            
            messagesContainer.appendChild(messageDiv);
            messagesContainer.scrollTop = messagesContainer.scrollHeight;
        }

        function showLoading(show) {
            document.getElementById('loading').style.display = show ? 'block' : 'none';
        }

        function updateStatus(type, message) {
            const indicator = document.getElementById('status-indicator');
            indicator.className = `status-indicator status-${type}`;
            indicator.textContent = message;
        }

        function handleKeyPress(event) {
            if (event.key === 'Enter') {
                sendMessage();
            }
        }

        function exportData() {
            const data = getCurrentFormData();
            
            // Create Excel-like export (CSV format)
            const csvContent = generateCSV(data);
            
            // Download the file
            const blob = new Blob([csvContent], { type: 'text/csv' });
            const url = window.URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = `SPT_Form_${new Date().toISOString().split('T')[0]}.csv`;
            a.click();
            window.URL.revokeObjectURL(url);
            
            addMessage('📄 Form data exported as CSV file!', 'ai');
        }

        function generateCSV(data) {
            let csv = 'Field,Value,Sheet,Cell\n';
            
            const cellMappings = {
                'project': 'Setup,C3',
                'project_number': 'Setup,C5', 
                'designer': 'Setup,F5',
                'point_load': 'Setup,D51',
                'min_bed_level': 'Setup,D73',
                'max_bed_level': 'Setup,D74',
                'material': 'Setup,E89',
                'diameter': 'Setup,F89',
                'wall_thickness': 'Setup,G89',
                'spring_type': 'SP1,C3',
                'friction_angle': 'SP1,J3'
            };
            
            Object.keys(data).forEach(field => {
                if (data[field]) {
                    const [sheet, cell] = cellMappings[field].split(',');
                    csv += `"${field}","${data[field]}","${sheet}","${cell}"\n`;
                }
            });
            
            return csv;
        }

        // Initialize
        document.addEventListener('DOMContentLoaded', function() {
            // Focus on chat input
            document.getElementById('chat-input').focus();
            
            // Add sample data for demonstration
            if (window.location.search.includes('demo=true')) {
                setTimeout(() => {
                    updateFormFields({
                        'project': 'Harbor Extension Project',
                        'designer': 'John Smith',
                        'material': 'S355',
                        'spring_type': 'API Sand'
                    });
                }, 1000);
            }
        });
    </script>
</body>
</html>
